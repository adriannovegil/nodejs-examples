// Declaramos y definimos un valor para la variable example.
var fruit = 'orange';
// Verificamos si la longitud de la cadena es mayor que 5.
if (fruit.length > 5) {
    console.log('The fruit name has more than five characters.');
} else {
    console.log('The fruit name has five characters or less.');
}