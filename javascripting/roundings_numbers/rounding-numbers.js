// Declaramos y definimos un valor para la variable example.
var roundUp = 1.5;
// Definimos una segunda variable a la que le asignamos el valor resultado de
// redondear el valor de la primera variable.
var rounded = Math.round(roundUp);
// Mostramos por pantalla el valor de la variable definida.
console.log(rounded);