// Declaramos y definimos un valor para la variable example.
var total = 0;
var limit = 10;
// Iteramos 10 veces
for (var i = 0; i < limit; i++) {
    // Le sumamos a la variable total el valor de i en la iteración actual.
    total += i;
}
// Mostramos por pantalla el valor de la variable definida.
console.log(total);